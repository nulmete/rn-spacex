import {
  MD3Colors,
  ActivityIndicator as RNPActivityIndicator,
} from "react-native-paper";

export const ActivityIndicator = () => {
  return <RNPActivityIndicator animating color={MD3Colors.primary0} />;
};
